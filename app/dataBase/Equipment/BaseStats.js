export default class BaseStats {
    constructor(id, name, hp, def, critRate){
        this.id = id
        this.name = name
        this.hp = hp
        this.def = def
        this.critRate = critRate
    }
}